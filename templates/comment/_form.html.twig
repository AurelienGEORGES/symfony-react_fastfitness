<div class="form-nouveau-commentaire">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-center my-4">
    <button class="btn btn-success">{{ button_label|default('Commenter') }}</button>
    </div>
{{ form_end(form) }}
</div>
