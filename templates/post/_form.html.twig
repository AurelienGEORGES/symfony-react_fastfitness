<div class="fs-2 text-white custom-form-nouvel-article">

{{ form_start(form) }}
    
    {{ form_widget(form) }}
   
    <div class="d-flex justify-content-center my-4">
    <button class="btn btn-success">{{ button_label|default('Enregister') }}</button>
    </div>
{{ form_end(form) }}
</div>